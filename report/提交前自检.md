# 提交前自查清单（任务书 9 节）

跑完下面这一遍命令做完整自检：

```bash
# 1. classes.txt 校验
wc -l classes.txt              # 必须恰好 50
head -3 classes.txt            # 头几个 ID
tail -3 classes.txt            # 尾几个 ID（应升序）
file -i classes.txt            # 必须 UTF-8

# 2. predictions.csv 校验
head -3 predictions.csv        # image_id,predicted_label,confidence
wc -l predictions.csv          # = 测试集图片数（不含表头）
# predicted_label 应是 4 位数字或 unknown；confidence ∈ [0,1]

# 3. 报告含分功
grep -E "个人贡献|分工|成员" report/实训报告.md
```

需要我帮你跑这些校验、然后把缺失项补上的话，告诉我"现在自检一遍"。
